package com.flightcentre.scheduler.model.enums;

/**
 * In the real world this would be a database table
 * with its own management endpoint, I decided to go  
 * with enum simply for type safety and simplicity 
 * within the scope of the assessment
 */
public enum Skill {
    FORKLIFT_CERTIFIED,
    FIRST_AID,
    SUPERVISOR
}
